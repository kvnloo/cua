# Issue 57

```text
snapshot -> build candidates -> chooser in run.py
                 |-> single_executable_candidate (not called)
                 |-> admit_guarded_run (not called)
                 |-> needs_visual_capture (not called)
```

Reusable helpers: none. Each function has one recipe.
