# Issue 47

Open. The sensitivity comparison was not run, so no shorter history policy is proposed.

https://github.com/kvnloo/cua/issues/47#issuecomment-5841889690.

`scripts/repro/handoff/issue-47-history.json`, from `chooser_projection.history_report`, keeps `selected_id` and `outcome`. `measured_success` is null. The proposal is `not justified`.
