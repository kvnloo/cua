# Issue 56

Before: `get_window_state`, `WalkBudget`, `verify_state`, and the jev-use visual path.

After: those same owners.

Eliminated: a second tree for passive rows, a conditional-skip service, another walk budget, a universal shadow store.
