# Issue 26

This issue is the draft pull request tracker, not one of the 69 `[RFC …]` packets. It stays open. It is not in the 69-row ledger.

Title: `test(cua-driver): one executable candidate before Jev`.

The branch is `test/rfc-fast-path-one-candidate-20260925`. The pull request is https://github.com/kvnloo/cua/pull/26. It stays a draft. It is not proposed for upstream merge.

The issue body already records the prototypes and the missing live trials. This note does not add a KEEP for #4 or #5. Live A/B numbers, native walker counts, and macOS or Windows censuses are not in the branch. Python/TypeScript parity on this branch is only the compiled-expectations pair. The other helpers were not mirrored.
