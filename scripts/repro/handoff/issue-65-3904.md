# Issue 65

Prepared comment for trycua/cua#3904. Not posted. No competing pull request.

The vectors below are the ones `test_passive_observation.py` runs.

Vector 1. `Row("calc-result", "6", passive=True)` beside `Row("calc-equals", "=", passive=False)`. `verification_text(rows, "calc-result")` returns `6`. `action_target` on the result row raises `AuthorityError`.

Vector 2. `action_target` on the equals row returns `calc-equals`.

These vectors show the caller rule. They are not a Calculator accessibility log.

Blocked for the native case. Missing machine: macOS. The trycua/cua#2958 Calculator session was not driven on this Linux host, so this comment does not claim that case passed.
