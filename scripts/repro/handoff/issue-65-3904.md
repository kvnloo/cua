# Issue 65

Comment for trycua/cua#3904. No competing pull request.

Vector 1: `Row("calc-result", "6", passive=True)`. `verification_text` returns `6`. `action_target` raises `AuthorityError`.

Vector 2: `Row("calc-equals", "=", passive=False)`. `action_target` returns `calc-equals`.

The native Calculator log is blocked. Missing machine: macOS.
