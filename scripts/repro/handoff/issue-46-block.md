# Issue 46

Open. No projection A/B currently exists, so no smallest-safe projection is claimed.

https://github.com/kvnloo/cua/issues/46#issuecomment-5841889526.

`scripts/repro/handoff/issue-46-projection.json` is the chooser shape from `chooser_projection.projection_report`. It keeps `id` and `description`, and receipts are `not produced`. That shape check is not the full-versus-projected outcome comparison.
