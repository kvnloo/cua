# Issue 24

The battery the issue asks for is `scripts/repro/handoff/issue-24-battery.json`, written by `task_battery.battery_table`. Five tasks and four arms are in that table. `live_success` is null and `wall_time_ms` is null because those columns were not measured.
