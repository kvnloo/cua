# Issue 8

Blocked. Missing machine: macOS.

The Calculator result in trycua/cua#2958 was not driven. This Linux host has no Calculator accessibility log. `passive_observation.py` can read a passive row and refuses to mint an action target. That unit rule is not the native log and not a patch recommendation with a passing Calculator run.
