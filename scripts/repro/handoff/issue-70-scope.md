# Issue 70

Scope lock for trycua/cua#3961: NO CHANGE NEEDED.

- `deterministic_fast_path.py` is not imported by the provider adapter.
- `guarded_run.py` is not imported by the provider adapter.
- `lazy_vision.py` is not imported by the provider adapter.

Policy stays with the caller.
